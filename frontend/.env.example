# Copy to .env.local for local overrides (optional).
# Vite only exposes variables prefixed with VITE_.

# Backend API origin when frontend and API run on different hosts (production).
# No trailing slash. Leave empty for local dev (Vite proxy uses /api → localhost:3000).
# VITE_API_URL=https://your-api.onrender.com
